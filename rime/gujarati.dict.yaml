# DEV ARTIFACT ONLY — not used at runtime (qjs translator + gu_lexicon_blob).
# Kept for offline export / debugging. Do not package or Deploy as a dependency.
# encoding: utf-8

---
name: gujarati
version: "5.1"
sort: by_weight
use_preset_vocabulary: false
...

# Intentionally empty — runtime lexicon lives in js/gu_lexicon_blob.json
